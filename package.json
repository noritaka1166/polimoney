{
  "name": "polimoney",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "start": "npm run start -w frontend",
    "format": "npm run format -w frontend",
    "check": "npm run check -w frontend",
    "fix": "npm run fix -w frontend"
  }
}
